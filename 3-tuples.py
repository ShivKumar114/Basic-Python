# TUPLE IS A IMMUTABLE DATA TYPE AS SAME AS LIST BUT THE ONLY DIFFERENCE IS THAT TUPULE IS IMMUTABLE.
# TUPLE KO () SE SHOW KRTE HAI - ( X,Y ,Z )
# TUPLE KE DIRECT METHODS SIRF 2 HI HAI count and index

name = (1,2,3,4,5)
print(type(name))   #output tupule ayyega

# AGAR HAME KOI AIISA TUPULE BANANA HAUI JISME SIRF 1 ELEMENT HO TOH 
name = (1)
print(type(name)) #output int ayyega because 1 hai sirf agar hame sirf 1 hi element chaiyte toh 1 ke agge ,
                  #comma lagana hoga

name = (1,)
print(type(name)) # AB OUTPUT TUPULE SHOW KREGA.                  
 